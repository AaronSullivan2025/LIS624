
## 2026-02-18 - Managing Software and Library Search<br>
<br>
__Goal__: Learn commands and usage surrounding Yaz client and searching with it <br>
<br>
__Context__: Meet the listed PASS requirements for the related assignment <br>
<br>
__Steps__:<br>
1. Updated system with 'sudo apt update' and 'sudo apt upgrade' 
2. Used 'apt search yaz' and then 'sudo apt install yaz' to install the yaz-client
3. Went through Google to find printable (hard copy) version of documentation on yaz-client
4. Started and experimented with yaz-client, concentrating on University of Kentucky catalog as per our documentation
5. Experimented with PQF (Prefix Query Format) notation
6. Used yaz-client with "-m" option to append bibliographic records to file
7. Created .marc and .json files as per the documentation's instructions
<br>
__Results__:<br>
There were some issues with the tldr package the class materials wanted us to find, but they were the same issues the instructor encountered when he was demonstrating, so I simply moved on.
<br>
No other issues cropped up. <br>


__Notes__: 

Reviewed and experimented with the following commands —
>sudo apt update
>apt search <package_name>
>apt show <package_name>
>sudo apt install <package_name>
>sudo apt --purge remove <package_name>
>sudo apt autoremove
>sudo apt clean
>sudo apt update
>sudo apt upgrade
>sudo apt autoremove
>sudo apt clean

